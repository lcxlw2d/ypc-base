import {ATTEND_SET_ATTENDLIST} from '../../actions/home/attend/attendAction';

module.exports = function homeReducer(state, action){
    var {type} = action;
    state = state || {};

    return state;
}
